package spider_ml

import (
	"carly_aws/pkg"
)

// handler is a simple function that takes a string and does a ToUpper.
func Handler(request pkg.SpiderMLEvent) (pkg.SpiderMLResponse, error) {
	return pkg.SpiderMLResponse{}, nil
}
